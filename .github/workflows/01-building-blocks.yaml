name : My workFlow
on : push


jobs:
   my-first-job:
      -runs-on: ubuntu-latest
      steps:
        -run: echo "Hello World!"
   my-second-job:
      -runs-on: ubuntu-latest
      steps:
        name: first-step
        -run: echo "I will fail"
        name: second-step
        -run: echo "Goodbye!"